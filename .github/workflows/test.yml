name: test
on:
  push:
    branches:
    - main
  pull_request:
    branches:
      - main
defaults: 
  run:
    shell: bash
jobs:
  Hello:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Hello world
        run: echo "helo world"
        
      - name: Remove theme.zip
        uses: JesseTG/rm@v1.0.3
        with:
         path: hello.py

  GoodBye:
    runs-on: ubuntu-latest
    steps:
      - name: good bye
        run: echo "good bye"
        shell: bash
      - name: Upload Release
        run: |
          cd ../..
          pwd
          
 
     
